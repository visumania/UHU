CREATE OR REPLACE PROCEDURE infoSocioActividad (pIdActividad IN ACTIVIDAD.IDACTIVIDAD%TYPE, cUser OUT SYS_REFCURSOR) IS
BEGIN
    OPEN cUser FOR
    SELECT NOMBRE, CORREO FROM SOCIO INNER JOIN REALIZA USING(NUMEROSOCIO) WHERE IDACTIVIDAD = pIdActividad;
END;